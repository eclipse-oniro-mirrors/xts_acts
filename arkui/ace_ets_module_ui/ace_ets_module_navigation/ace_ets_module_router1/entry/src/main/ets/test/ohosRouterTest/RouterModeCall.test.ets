/**
 * Copyright (c) 2025 Shenzhen Kaihong Digital Industry Development Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

import router from '@ohos.router';
import { describe, beforeAll, beforeEach, afterEach, afterAll, it, expect, Level } from '@ohos/hypium';
import Utils from '../../MainAbility/common/Utils'
import { Component, Driver, UiWindow, ON, MatchPattern, DisplayRotation, ResizeDirection, WindowMode, PointerMatrix} from '@ohos.UiTest';

export default function RouterModeCall() {
  describe('RouterModeCall', () => {
    beforeEach(async (done: Function) => {
      console.info("flex beforeEach start");
      let options: router.RouterOptions = {
        url: 'MainAbility/pages/ohosRouter/RouterModeCall',
      }
      try {
        router.clear();
        let pages = router.getState();
        console.info('get router state success ' + JSON.stringify(pages));
        if (!('RouterModeCall' == pages.name)) {
          console.info('get router state success ' + JSON.stringify(pages.name));
          let result = await router.pushUrl(options);
          await Utils.sleep(2000);
          console.info('push router page success ' + JSON.stringify(result));
        }
      } catch (err) {
        console.error('push router page error: ' + err);
      }
      done()
    });

    afterEach(async () => {
      await Utils.sleep(1000);
      console.info('RouterModeCall after each called');
    });

    /**
     * @tc.name   RouterModeCall_0400
     * @tc.number RouterModeCall_0400
     * @tc.desc   Test Router pushUrl callback 401
     * @tc.type   FUNCTION
     * @tc.size   MEDIUMTEST
     * @tc.level  LEVEL2
     */
    it('RouterModeCall_0400', Level.LEVEL2, async (done: Function) => {
      console.info('RouterModeCall_0400 START');
      await Utils.sleep(1000);

      let driver = Driver.create();
      await driver.delayMs(1000);

      let btn1: Component = await driver.findComponent(ON.id('401Btn2'));
      await btn1.click()
      console.info('[RouterModeCall_0400] btn1 click');
      await driver.delayMs(2000);

      let code: string | undefined = AppStorage.get('401Code')
      expect(code).assertEqual('401');
      AppStorage.delete('401Code'); 
      console.info('[RouterModeCall_0400] END');
      done();
    });

    /**
     * @tc.name   RouterModeCall_0500
     * @tc.number RouterModeCall_0500
     * @tc.desc   Test Router pushUrl callback 100002
     * @tc.type   FUNCTION
     * @tc.size   MEDIUMTEST
     * @tc.level  LEVEL2
     */
    it('RouterModeCall_0500', Level.LEVEL2, async (done: Function) => {
      console.info('RouterModeCall_0500 START');
      await Utils.sleep(1000);

      let driver = Driver.create();
      await driver.delayMs(1000);

      let btn1: Component = await driver.findComponent(ON.id('100002Btn2'));
      await btn1.click()
      console.info('[RouterModeCall_0500] btn1 click');
      await driver.delayMs(2000);

      let code: string | undefined = AppStorage.get('100002Code')
      expect(code).assertEqual('100002');
      AppStorage.delete('100002Code'); 
      console.info('[RouterModeCall_0500] END');
      done();
    });

    /**
     * @tc.name   RouterModeCall_0600
     * @tc.number RouterModeCall_0600
     * @tc.desc   Test Router pushUrl 100003
     * @tc.type   FUNCTION
     * @tc.size   MEDIUMTEST
     * @tc.level  LEVEL2
     */
    it('RouterModeCall_0600', Level.LEVEL2, async (done: Function) => {
      console.info('RouterModeCall_0600 START');
      await Utils.sleep(1000);

      let driver = Driver.create();
      await driver.delayMs(1000);

      let btn1: Component = await driver.findComponent(ON.text('routerCallbackPush100003'));
      await btn1.click()
      console.info('[RouterModeCall_0600] btn1 click');
      await driver.delayMs(3000);

      let code: string | undefined = AppStorage.get('100003Code')
      expect(code).assertEqual('100003');
      AppStorage.delete('100003Code'); 
      console.info('[RouterModeCall_0600] END');
      done();
    });


  })
}
